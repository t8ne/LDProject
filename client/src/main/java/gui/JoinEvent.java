package gui;
/**
 * A classe JoinEvent implementa a interface Runnable para definir um evento de junção.
 * Esta classe pode ser usada para representar a lógica de um evento que ocorre em uma thread separada.
 */
public class JoinEvent implements Runnable {
	/**
	 * Sobrescreve o método run() da interface Runnable.
	 * Define a lógica que será executada quando a thread associada a este evento for iniciada.
	 */
	@Override
	public void run() {
		
	}

}
